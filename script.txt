-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.candidato_evento (
  id integer NOT NULL DEFAULT nextval('candidato_evento_id_seq'::regclass),
  candidato_id uuid NOT NULL,
  evento_id integer NOT NULL,
  tipo_reunion_id integer NOT NULL,
  asignado_at timestamp with time zone NOT NULL DEFAULT now(),
  estado text NOT NULL DEFAULT 'CONFIRMADO'::text CHECK (estado = ANY (ARRAY['CONFIRMADO'::text, 'CANCELADO'::text])),
  CONSTRAINT candidato_evento_pkey PRIMARY KEY (id),
  CONSTRAINT candidato_evento_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT candidato_evento_evento_id_fkey FOREIGN KEY (evento_id) REFERENCES public.eventos(id),
  CONSTRAINT candidato_evento_tipo_reunion_id_fkey FOREIGN KEY (tipo_reunion_id) REFERENCES public.tipos_reunion(id)
);
CREATE TABLE public.candidato_fase (
  candidato_id uuid NOT NULL,
  tipo_reunion_id integer NOT NULL,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT candidato_fase_pkey PRIMARY KEY (candidato_id),
  CONSTRAINT candidato_fase_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT candidato_fase_tipo_reunion_id_fkey FOREIGN KEY (tipo_reunion_id) REFERENCES public.tipos_reunion(id)
);
CREATE TABLE public.candidato_gestion (
  candidato_id uuid NOT NULL,
  estado_gestion_id integer NOT NULL,
  medio_comunicacion_id integer,
  ultimo_contacto timestamp with time zone,
  intentos_llamada integer NOT NULL DEFAULT 0,
  hora_preferida time without time zone,
  nota_horario text,
  CONSTRAINT candidato_gestion_pkey PRIMARY KEY (candidato_id),
  CONSTRAINT candidato_gestion_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT candidato_gestion_estado_gestion_id_fkey FOREIGN KEY (estado_gestion_id) REFERENCES public.estados_gestion(id),
  CONSTRAINT candidato_gestion_medio_comunicacion_id_fkey FOREIGN KEY (medio_comunicacion_id) REFERENCES public.medios_comunicacion(id)
);
CREATE TABLE public.candidato_ideal (
  candidato_id uuid NOT NULL,
  ci_jornada integer,
  ci_edad integer,
  ci_ubicacion integer,
  ci_estudios integer,
  ci_estrato integer,
  ci_nivel_prog integer,
  ci_ocupacion integer,
  ci_total integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT candidato_ideal_pkey PRIMARY KEY (candidato_id),
  CONSTRAINT candidato_ideal_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id)
);
CREATE TABLE public.candidato_perfil (
  candidato_id uuid NOT NULL,
  nivel_educativo_id integer,
  titulo text,
  conocimiento_prog_id integer,
  ocupacion_id integer,
  tipo_convenio_id integer,
  CONSTRAINT candidato_perfil_pkey PRIMARY KEY (candidato_id),
  CONSTRAINT candidato_perfil_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT candidato_perfil_nivel_educativo_id_fkey FOREIGN KEY (nivel_educativo_id) REFERENCES public.niveles_educativos(id),
  CONSTRAINT candidato_perfil_conocimiento_prog_id_fkey FOREIGN KEY (conocimiento_prog_id) REFERENCES public.conocimientos_programacion(id),
  CONSTRAINT candidato_perfil_ocupacion_id_fkey FOREIGN KEY (ocupacion_id) REFERENCES public.ocupaciones(id),
  CONSTRAINT candidato_perfil_tipo_convenio_id_fkey FOREIGN KEY (tipo_convenio_id) REFERENCES public.tipos_convenio(id)
);
CREATE TABLE public.candidato_ubicacion (
  candidato_id uuid NOT NULL,
  departamento_id integer NOT NULL,
  municipio_id integer NOT NULL,
  sede_interes_id integer NOT NULL,
  estrato_id integer NOT NULL,
  CONSTRAINT candidato_ubicacion_pkey PRIMARY KEY (candidato_id),
  CONSTRAINT candidato_ubicacion_municipio_id_fkey FOREIGN KEY (municipio_id) REFERENCES public.municipios(id),
  CONSTRAINT candidato_ubicacion_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT candidato_ubicacion_departamento_id_fkey FOREIGN KEY (departamento_id) REFERENCES public.departamentos(id),
  CONSTRAINT candidato_ubicacion_sede_interes_id_fkey FOREIGN KEY (sede_interes_id) REFERENCES public.sedes(id),
  CONSTRAINT candidato_ubicacion_estrato_id_fkey FOREIGN KEY (estrato_id) REFERENCES public.estratos(id)
);
CREATE TABLE public.candidatos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre text NOT NULL,
  apellido text NOT NULL,
  correo text NOT NULL UNIQUE,
  telefono text NOT NULL,
  fecha_nacimiento date NOT NULL,
  tipo_documento_id integer NOT NULL,
  numero_documento text NOT NULL UNIQUE,
  genero_id integer NOT NULL,
  discord_usuario text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT candidatos_pkey PRIMARY KEY (id),
  CONSTRAINT candidatos_tipo_documento_id_fkey FOREIGN KEY (tipo_documento_id) REFERENCES public.tipos_documento(id),
  CONSTRAINT candidatos_genero_id_fkey FOREIGN KEY (genero_id) REFERENCES public.generos(id)
);
CREATE TABLE public.conocimientos_programacion (
  id integer NOT NULL DEFAULT nextval('conocimientos_programacion_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  nivel integer NOT NULL,
  CONSTRAINT conocimientos_programacion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.departamentos (
  id integer NOT NULL DEFAULT nextval('departamentos_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  nombre text NOT NULL,
  CONSTRAINT departamentos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.estados_gestion (
  id integer NOT NULL DEFAULT nextval('estados_gestion_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT estados_gestion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.estratos (
  id integer NOT NULL DEFAULT nextval('estratos_id_seq'::regclass),
  valor integer NOT NULL UNIQUE CHECK (valor >= 1 AND valor <= 6),
  CONSTRAINT estratos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.eventos (
  id integer NOT NULL DEFAULT nextval('eventos_id_seq'::regclass),
  tipo_reunion_id integer NOT NULL,
  sede_id integer,
  horario_id integer,
  fecha_hora timestamp with time zone NOT NULL,
  inscritos_actuales integer NOT NULL DEFAULT 0,
  capacidad_total integer NOT NULL,
  estado text NOT NULL DEFAULT 'DISPONIBLE'::text CHECK (estado = ANY (ARRAY['DISPONIBLE'::text, 'COMPLETO'::text, 'CANCELADO'::text])),
  descripcion text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT eventos_pkey PRIMARY KEY (id),
  CONSTRAINT eventos_tipo_reunion_id_fkey FOREIGN KEY (tipo_reunion_id) REFERENCES public.tipos_reunion(id),
  CONSTRAINT eventos_sede_id_fkey FOREIGN KEY (sede_id) REFERENCES public.sedes(id),
  CONSTRAINT eventos_horario_id_fkey FOREIGN KEY (horario_id) REFERENCES public.horarios(id)
);
CREATE TABLE public.generos (
  id integer NOT NULL DEFAULT nextval('generos_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT generos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.horarios (
  id integer NOT NULL DEFAULT nextval('horarios_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  hora_inicio time without time zone NOT NULL,
  hora_fin time without time zone NOT NULL,
  CONSTRAINT horarios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.llamadas (
  id bigint NOT NULL DEFAULT nextval('llamadas_id_seq'::regclass),
  candidato_id uuid NOT NULL,
  fecha_hora timestamp with time zone NOT NULL DEFAULT now(),
  resultado_id integer NOT NULL,
  franja text NOT NULL CHECK (franja = ANY (ARRAY['manana'::text, 'tarde'::text, 'noche'::text])),
  conversation_id text,
  duracion_segundos integer,
  nota text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT llamadas_pkey PRIMARY KEY (id),
  CONSTRAINT llamadas_candidato_id_fkey FOREIGN KEY (candidato_id) REFERENCES public.candidatos(id),
  CONSTRAINT llamadas_resultado_id_fkey FOREIGN KEY (resultado_id) REFERENCES public.resultados_llamada(id)
);
CREATE TABLE public.medios_comunicacion (
  id integer NOT NULL DEFAULT nextval('medios_comunicacion_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT medios_comunicacion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.municipios (
  id integer NOT NULL DEFAULT nextval('municipios_id_seq'::regclass),
  departamento_id integer NOT NULL,
  nombre text NOT NULL,
  CONSTRAINT municipios_pkey PRIMARY KEY (id),
  CONSTRAINT municipios_departamento_id_fkey FOREIGN KEY (departamento_id) REFERENCES public.departamentos(id)
);
CREATE TABLE public.niveles_educativos (
  id integer NOT NULL DEFAULT nextval('niveles_educativos_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  orden integer NOT NULL,
  CONSTRAINT niveles_educativos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.ocupaciones (
  id integer NOT NULL DEFAULT nextval('ocupaciones_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT ocupaciones_pkey PRIMARY KEY (id)
);
CREATE TABLE public.resultados_llamada (
  id integer NOT NULL DEFAULT nextval('resultados_llamada_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT resultados_llamada_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sedes (
  id integer NOT NULL DEFAULT nextval('sedes_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  nombre text NOT NULL,
  CONSTRAINT sedes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tipos_convenio (
  id integer NOT NULL DEFAULT nextval('tipos_convenio_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT tipos_convenio_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tipos_documento (
  id integer NOT NULL DEFAULT nextval('tipos_documento_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT tipos_documento_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tipos_reunion (
  id integer NOT NULL DEFAULT nextval('tipos_reunion_id_seq'::regclass),
  codigo text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  CONSTRAINT tipos_reunion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.webhook_logs (
  id integer NOT NULL DEFAULT nextval('webhook_logs_id_seq'::regclass),
  payload jsonb,
  recibido_en timestamp with time zone NOT NULL DEFAULT now(),
  procesado_exitosamente boolean DEFAULT false,
  error_log text,
  CONSTRAINT webhook_logs_pkey PRIMARY KEY (id)
);